---
applications:
    - name: live-fraud-detector
      memory: 256M
      instances: 1
      command: npm start
      buildpacks:
          - nodejs_buildpack
      timeout: 15
      path: .
      disk_quota: 1024M
      stack: cflinuxfs3
      env:
          NODE_ENV: production
          MONGO_USER: mamadou
          MONGO_PASSWORD: cireguisse
          MONGO_DB: retail
          HOST_API: 0.0.0.0
          PORT_BACKEND: 8080
      routes:
        - route: live-fraud-detector.eu-gb.mybluemix.net
